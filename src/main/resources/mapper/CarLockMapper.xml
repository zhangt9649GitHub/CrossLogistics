<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.siruiman.crosslogistics.mapper.CarLockMapper" >
  <resultMap id="BaseResultMap" type="com.siruiman.crosslogistics.model.CarLock" >
    <!--
      WARNING - @mbggenerated
      This element is automatically generated by MyBatis Generator, do not modify.
    -->
    <id column="lock_id" property="lockId" jdbcType="INTEGER" />
    <result column="car_id" property="carId" jdbcType="INTEGER" />
    <result column="state" property="state" jdbcType="TINYINT" />
    <result column="lock_number" property="lockNumber" jdbcType="VARCHAR" />
    <result column="base_station_number" property="baseStationNumber" jdbcType="VARCHAR" />
    <result column="status" property="status" jdbcType="TINYINT" />
    <result column="lock_position" property="lockPosition" jdbcType="INTEGER" />
  </resultMap>
  <delete id="deleteByPrimaryKey" parameterType="java.lang.Integer" >
    <!--
      WARNING - @mbggenerated
      This element is automatically generated by MyBatis Generator, do not modify.
    -->
    delete from car_lock
    where lock_id = #{lockId,jdbcType=INTEGER}
  </delete>
  <insert id="insert" parameterType="com.siruiman.crosslogistics.model.CarLock" >
    <!--
      WARNING - @mbggenerated
      This element is automatically generated by MyBatis Generator, do not modify.
    -->
    <selectKey resultType="java.lang.Integer" keyProperty="lockId" order="AFTER" >
      SELECT LAST_INSERT_ID()
    </selectKey>
    insert into car_lock (car_id, `state`, lock_number, 
      base_station_number, `status`, lock_position
      )
    values (#{carId,jdbcType=INTEGER}, #{state,jdbcType=TINYINT}, #{lockNumber,jdbcType=VARCHAR}, 
      #{baseStationNumber,jdbcType=VARCHAR}, #{status,jdbcType=TINYINT}, #{lockPosition,jdbcType=INTEGER}
      )
  </insert>
  <update id="updateByPrimaryKey" parameterType="com.siruiman.crosslogistics.model.CarLock" >
    <!--
      WARNING - @mbggenerated
      This element is automatically generated by MyBatis Generator, do not modify.
    -->
    update car_lock
    set car_id = #{carId,jdbcType=INTEGER},
      `state` = #{state,jdbcType=TINYINT},
      lock_number = #{lockNumber,jdbcType=VARCHAR},
      base_station_number = #{baseStationNumber,jdbcType=VARCHAR},
      `status` = #{status,jdbcType=TINYINT},
      lock_position = #{lockPosition,jdbcType=INTEGER}
    where lock_id = #{lockId,jdbcType=INTEGER}
  </update>
  <select id="selectByPrimaryKey" resultMap="BaseResultMap" parameterType="java.lang.Integer" >
    <!--
      WARNING - @mbggenerated
      This element is automatically generated by MyBatis Generator, do not modify.
    -->
    select lock_id, car_id, `state`, lock_number, base_station_number, `status`, lock_position
    from car_lock
    where lock_id = #{lockId,jdbcType=INTEGER}
  </select>
  <select id="selectAll" resultMap="BaseResultMap" >
    <!--
      WARNING - @mbggenerated
      This element is automatically generated by MyBatis Generator, do not modify.
    -->
    select lock_id, car_id, `state`, lock_number, base_station_number, `status`, lock_position
    from car_lock
  </select>

  <select id="selectCarLockByCarId" resultMap="BaseResultMap" parameterType="java.lang.Integer">
    <!--
      WARNING - @mbggenerated
      This element is automatically generated by MyBatis Generator, do not modify.
    -->
    select lock_id, car_id, `state`, lock_number, base_station_number, `status`, lock_position
    from car_lock
    where car_id = #{carId}
  </select>
  <select id="selectCarLockByLockNumber" resultMap="BaseResultMap" parameterType="java.lang.String">
    <!--
      WARNING - @mbggenerated
      This element is automatically generated by MyBatis Generator, do not modify.
    -->
    select lock_id, car_id, `state`, lock_number, base_station_number, `status`, lock_position
    from car_lock
    where lock_number = #{lockNumber}
  </select>

  <select id="selectCarLock" resultMap="BaseResultMap">
    <!--
      WARNING - @mbggenerated
      This element is automatically generated by MyBatis Generator, do not modify.
    -->
    select lock_id, car_id, `state`, lock_number, base_station_number, `status`, lock_position
    from car_lock
    where car_id = #{carId} and lock_position = #{lockPosition}
  </select>

  <select id="selectAllCarLockState" resultMap="BaseResultMap">
    select  `state`, lock_number from car_lock
  </select>

  <update id="updateUnLockState" parameterType="com.siruiman.crosslogistics.model.CarLock">
    update car_lock
    set  `state` =#{lock.state},
     update_time =#{lock.updateTime}
    where lock_number =#{lock.lockNumber}
  </update>
  <update id="updateLockStationNum" parameterType="com.siruiman.crosslogistics.model.CarLock">
    update car_lock
    set  base_station_number =#{lock.baseStationNumber},
          electricity =#{lock.electricity},
          `state` =#{lock.state},
          lac =#{lock.lac},
          update_time =#{lock.updateTime},
          sign =#{lock.sign}
    where lock_number =#{lock.lockNumber}
  </update>
  <update id="updateLockLatLng" parameterType="com.siruiman.crosslogistics.model.CarLock">
     update car_lock
        set electricity =#{lock.electricity},
            lat =#{lock.lat},
            lng=#{lock.lng},
            `state` =#{lock.state},
             update_time =#{lock.updateTime}
      where lock_number =#{lock.lockNumber}
  </update>

  <select id="selectVacancyCarLock" resultMap="BaseResultMap" >
    <!--
      WARNING - @mbggenerated
      This element is automatically generated by MyBatis Generator, do not modify.
    -->
    select lock_id, car_id, `state`, lock_number, base_station_number, `status`, lock_position
    from car_lock where lock_position =#{lockPostion} AND `status` = #{status}
  </select>
  <select id="selectCarIdByLockNumber" resultMap="BaseResultMap" >
    <!--
      WARNING - @mbggenerated
      This element is automatically generated by MyBatis Generator, do not modify.
    -->
    select lock_id, car_id, `state`, lock_number, base_station_number, `status`, lock_position
    from car_lock where lock_number =#{lockNumber}
  </select>

  <select id="getList" resultMap="BaseResultMap">
    SELECT
	  clk.lock_id,clk.lock_number,car.car_number,clk.electricity,clk.lock_position,clk.`status`
    FROM
	  car_lock AS clk
    LEFT JOIN car ON clk.car_id = car.car_id
    where 1=1
    <if test="lockNumber!=null and lockNumber !=''">
     and clk.lock_number = #{lockNumber}
    </if>
    <if test="status!=null and status !=''">
     and  clk.`status` = #{status}
    </if>
    order by clk.lock_id desc
  </select>
  <select id="getCount" resultType="integer">
    SELECT
       count(clk.lock_id)
    FROM
    car_lock AS clk
    LEFT JOIN car ON clk.car_id = car.car_id
    where 1=1
    <if test="lockNumber!=null and lockNumber !=''">
     and clk.lock_number = #{lockNumber}
    </if>
    <if test="status!=null and status !=''">
     and clk.`status` = #{status}
    </if>
  </select>
  <update id="updateCarNumberAndPosition" parameterType="com.siruiman.crosslogistics.model.CarLock">
    update car_lock
    set Lock_number =#{lockNumber},
    lock_position=#{lockPosition},
    update_time =#{updateTime}
    where lock_id = #{lockId}
  </update>

  <update id="scrapLock" >
    update car_lock
    set
    `status` = 2 ,
    update_time =#{updateTime}
    where lock_id = #{lockId}
  </update>
  <update id="repairLock" >
    update car_lock
    set
    `status` = 3 ,
    update_time =#{updateTime}
    where lock_id = #{lockId}
  </update>
  <insert id="addLock" parameterType="com.siruiman.crosslogistics.model.dto.CarLockDto" >
    <!--
      WARNING - @mbggenerated
      This element is automatically generated by MyBatis Generator, do not modify.
    -->
    <selectKey resultType="java.lang.Integer" keyProperty="lockId" order="AFTER" >
      SELECT LAST_INSERT_ID()
    </selectKey>
    insert into car_lock ( lock_number,`status`,lock_position)
    values (#{lockDto.lockNumber},3,#{lockDto.lockPosition})
  </insert>
  <select id="selectCarLockByLockNumberORId" resultMap="BaseResultMap">
    select * from car_lock
     where Lock_number =  #{lockNumber}
     <if test="lockId !=null and lockId !=''">
      and lock_id = #{lockId}
     </if>
  </select>
</mapper>