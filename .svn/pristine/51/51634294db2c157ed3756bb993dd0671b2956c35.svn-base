package com.siruiman.crosslogistics.zhangzhanwei;

import com.siruiman.crosslogistics.util.DateUtil;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.context.junit4.SpringRunner;

import java.util.ArrayList;

/**
 * @author 张占伟
 * @date 2019/1/13 11:15
 */

@SpringBootTest
@RunWith(SpringRunner.class)
public class WXPayTest {
/*
    @Autowired
    private IAliPayResultsService iAliPayResultsService;

    @Test
    public void test(){
        IAliPayResults results = new IAliPayResults();
        results.setApp_id("2315123");
        results.setBuyer_id("3256");
        results.setTrade_no("342652");
        results.setGmt_close(DateUtil.getDateTime());
        results.setGmt_refund(DateUtil.getDateTime());
        results.setGmt_create(DateUtil.getDateTime());
        iAliPayResultsService.insert(results);
    }*/

    @Test
    public void  test2(){
        ArrayList<Object> list = new ArrayList<>();
        System.out.println(list.size());
    }
}
